server:
  port: 9003
spring:
  application:
    name: leadnews-wemedia

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8850
      config:
        server-addr: 127.0.0.1:8850  # 配置中心地址
        file-extension: yml  # 文件后缀名
